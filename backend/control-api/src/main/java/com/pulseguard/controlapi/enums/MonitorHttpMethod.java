package com.pulseguard.controlapi.enums;

/**
 * HTTP method used when checking a monitored endpoint.
 *
 * <p>Only GET is supported by the MVP. Additional methods are added when a
 * requirement actually needs them.
 */
public enum MonitorHttpMethod {
    GET
}
