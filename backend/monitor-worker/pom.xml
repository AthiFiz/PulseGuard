<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.0</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>

	<groupId>com.pulseguard</groupId>
	<artifactId>monitor-worker</artifactId>
	<version>0.0.1-SNAPSHOT</version>

	<name>monitor-worker</name>
	<description>PulseGuard Monitor Worker - background monitoring application</description>

	<properties>
		<java.version>21</java.version>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

		<!--
			Quality tooling (Task 13). None of this runs during an ordinary build:
			the versions are only pinned here, and coverage lives behind the
			`coverage` profile below. `./mvnw clean verify` never contacts SonarQube.
		-->
		<jacoco.version>0.8.13</jacoco.version>
		<sonar.scanner.version>5.1.0.4751</sonar.scanner.version>

		<sonar.projectKey>pulseguard-monitor-worker</sonar.projectKey>
		<sonar.projectName>PulseGuard Monitor Worker</sonar.projectName>
		<sonar.coverage.jacoco.xmlReportPaths>${project.build.directory}/site/jacoco/jacoco.xml</sonar.coverage.jacoco.xmlReportPaths>

		<!--
			Coverage exclusions only — these files are still statically analysed for
			bugs, vulnerabilities and smells. The list is deliberately conservative:
			bootstrap, records and enum constants have nothing worth asserting.
			domain/** is deliberately NOT excluded here: Incident.resolve() and
			OutboxEvent.markPublished()/markFailed() are real state transitions.
			No service, security, monitoring, outbox, event or email code is excluded.
		-->
		<sonar.coverage.exclusions>**/MonitorWorkerApplication.java,**/dto/**,**/enums/**</sonar.coverage.exclusions>
	</properties>

	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>
		<!--
			Turns the metrics Micrometer already collects into the text format
			Prometheus scrapes, at /actuator/prometheus. Version is managed by
			the Spring Boot parent, so it cannot drift from the Micrometer core
			that Actuator itself pulls in.
		-->
		<dependency>
			<groupId>io.micrometer</groupId>
			<artifactId>micrometer-registry-prometheus</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>

		<!--
			The worker reads and writes the same MySQL database as the Control
			API, but owns no migrations: Flyway stays entirely in control-api,
			so there is deliberately no Flyway dependency here.
		-->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>com.mysql</groupId>
			<artifactId>mysql-connector-j</artifactId>
			<scope>runtime</scope>
		</dependency>

		<!--
			Kafka lives only in the worker. The Control API and the frontend
			have no producer, no consumer and no broker dependency: incidents
			are announced by whoever observed them.
		-->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-kafka</artifactId>
		</dependency>

		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<pluginManagement>
			<plugins>
				<!--
					Pins the scanner version for `./mvnw sonar:sonar`. Declared in
					pluginManagement rather than plugins so it is never bound to a
					phase — an ordinary build does not know it exists.
				-->
				<plugin>
					<groupId>org.sonarsource.scanner.maven</groupId>
					<artifactId>sonar-maven-plugin</artifactId>
					<version>${sonar.scanner.version}</version>
				</plugin>
			</plugins>
		</pluginManagement>

		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<executions>
					<execution>
						<id>default-compile</id>
						<phase>compile</phase>
						<goals>
							<goal>compile</goal>
						</goals>
						<configuration>
							<annotationProcessorPaths>
								<path>
									<groupId>org.projectlombok</groupId>
									<artifactId>lombok</artifactId>
								</path>
							</annotationProcessorPaths>
						</configuration>
					</execution>
					<execution>
						<id>default-testCompile</id>
						<phase>test-compile</phase>
						<goals>
							<goal>testCompile</goal>
						</goals>
						<configuration>
							<annotationProcessorPaths>
								<path>
									<groupId>org.projectlombok</groupId>
									<artifactId>lombok</artifactId>
								</path>
							</annotationProcessorPaths>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

	<profiles>
		<!--
			Coverage is opt-in. `./mvnw clean verify` runs exactly as before and
			produces no coverage report; `./mvnw clean verify -Pcoverage` attaches
			the JaCoCo agent and writes target/site/jacoco/jacoco.xml, which is what
			SonarQube reads. Keeping it out of the default build means the everyday
			edit-test loop pays nothing for a report nobody is looking at.
		-->
		<profile>
			<id>coverage</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.jacoco</groupId>
						<artifactId>jacoco-maven-plugin</artifactId>
						<version>${jacoco.version}</version>
						<executions>
							<execution>
								<id>jacoco-prepare-agent</id>
								<goals>
									<goal>prepare-agent</goal>
								</goals>
							</execution>
							<execution>
								<id>jacoco-report</id>
								<phase>verify</phase>
								<goals>
									<goal>report</goal>
								</goals>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>

</project>
